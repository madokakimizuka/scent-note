<ul>
  <% comments.each do |comment| %>
    <% if comment.id.present? %>
      <li>
        <p><%= comment.content %></p>
        <%# デリートがあると自動でリロードされない %>
        <%#= link_to "Delete", note_comment_path(comment.id), method: :delete, data: { confirm: "削除してよろしいですか？" } %>
      </li>
    <% end %>
  <% end %>
</ul>
